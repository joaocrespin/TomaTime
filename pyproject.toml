[project]
name = "tomatime"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "joaocrespin", email = "" }
]
requires-python = ">=3.14"
dependencies = [
    "playsound3>=3.3.2",
    "plyer>=2.1.0",
    "pytest>=9.1.1",
]

[project.scripts]
pomodoro = "tomatime:main"

[build-system]
requires = ["uv_build>=0.12.4,<0.13.0"]
build-backend = "uv_build"
